# 健康检查 2026-06-25

> 知识库首次健康检查。按 [[outputs/health/README]] 的 6 项清单逐条核查。
> 检查时点统计：raw 资料 23 篇 · 来源摘要页 23 篇 · 概念页 12 个 · 实体页 1 个 · 综合页 0。

---

## 总评

整体健康 ✅。资料与摘要页 1:1 全覆盖、无孤立页、无断链、无 clippings 残留。
仅有 2 个**轻微**问题（标签格式、模型版本号过期风险）和 1 个**结构空缺**（综合页为空），均不影响使用，建议后续处理。

---

## 1. 缺来源摘要的 raw 资料

**结果：无 ✅**

raw/ 共 23 篇资料，wiki/sources/ 共 23 篇摘要页，一一对应，无遗漏。

## 2. 缺来源的页面

**结果：无 ✅**

12 个概念页 + 1 个实体页均含来源链接（指向对应 `[[claude-howto-*]]` 摘要页）。
重要判断处也保留了来源指向。

## 3. 重复或冲突的概念

**结果：无明显重复/冲突 ✅**

12 个概念页主题各不相同：`claude-code`（总览）+ 10 个功能模块页 + `claude-code-功能组合工作流`（横切主题）。
- `claude-code` 与各功能页是「总览 ↔ 专题」层级关系，不算重复。
- 未发现互相矛盾的表述。

## 4. 没有链接的孤立页面

**结果：无孤立页 ✅**

所有 wiki 页面均至少有一个入链（来自 index、概念页或来源摘要页）。
注：`index.md` 作为入口页，由 `[[wiki/index]]` 等导航引用，属正常。

## 5. 可能过期的判断 ⚠️

含「待核验/待补充」标注的页面共 13 个（concepts 6 + sources 6 + log）。其中**最需要关注**的是：

- **模型版本号已可能过期**：多处来源（01-slash-commands、09-advanced-features、10-cli、主指南）提到
  `Opus 4.6 / Sonnet 4.6 / Haiku 4.5`，原文写于 2026-04。
  **与当前环境冲突**：本次会话运行环境为 **Opus 4.8**，说明已有更新版本。
  → 建议：这些版本号视为「历史快照」，引用时注明「截至原文 2026-04」，不要当最新事实。
- 其它 `待核验`：MCP 增大 token 上限的命令写法（原文为占位）、部分 CLI 参数
  （`--unsafe`/`--dry-run`/`--list-sessions`）、`/effort max` 与模型版本的对应关系、
  Memory 的 `claudeMdExcludes` 确切键名。这些待将来对照官方文档核实。

→ 这些标注本身是**好现象**（说明没编造），无需立即修改，留待对照 code.claude.com 官方文档时一并核验。

## 6. 目录与标签不一致 ⚠️

**标签值：全部正确 ✅**　**格式：轻微不一致**

- raw/clips/ 下 23 篇文件标签值均为 `clip`，与目录匹配；无 `article`/`paper` 误用；无 `clippings` 残留。
- **轻微格式不一致**：8 个 subagent 定义文件的来源信息写在顶部 HTML 注释里，标签格式是
  `tags: clip`（非数组），其余 15 篇是 `tags: [clip]`（数组）。
  - 原因：subagent 原文自带 frontmatter（name/tools/model），为避免 YAML 冲突，来源信息以注释保存。
  - 影响：极小。这些标签在 HTML 注释内，Obsidian 不会将其解析为真实标签；标签值也正确。
  - → 可选处理：统一改为 `tags: [clip]` 以保持书写一致，或保持现状（不影响功能）。

---

## 处理建议 / 下一步

| 优先级 | 事项 |
| --- | --- |
| 低 | 模型版本号引用时标注「截至 2026-04」，避免误当最新 |
| 低 | 统一 8 个 subagent 文件注释里的 `tags: clip` → `tags: [clip]`（纯一致性，可选） |
| 结构 | `wiki/syntheses/` 仍为空，建议建一篇《Claude Code 全景图》综合页，把 12 个概念串成体系 |
| 待办 | 多处 `待核验` 项，待引入 code.claude.com 官方文档来源时一并核实 |

> 本次为只读审计，未改动任何知识内容。
